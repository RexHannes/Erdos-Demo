# AGENTS.md

This folder is for reproducible blocker-baseline experiments related to Erdős Problem #287.

Do not claim a proof of Erdős #287.

Do not mark a blocker as proved unless all of the following are explicit:
- trigger number near `M`;
- obstruction prime `q`;
- forced denominator pattern;
- why the gap condition forces that pattern;
- maximal `q`-adic layer;
- residue contradiction;
- valid `M`-range.

Keep `proved`, `candidate`, and `observed` labels separate.

Always output holes tables when scanning a chain or candidate blocker family.

Prefer exact integer arithmetic. If probabilistic or heuristic checks are ever introduced, label them clearly as heuristic and keep them out of proved claims.
